import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mqtt-app-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
